require 'spec_helper'

describe Omniauth::Wattpad do
  it 'has a version number' do
    expect(Omniauth::Wattpad::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
